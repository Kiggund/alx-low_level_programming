#include "main.h"

/**
 *  rev_string - reverses a string
 *  @s: input
 *  Return: string in reverse
 */
void rev_string(char *s)
{
	char rv = s[0];
	int c = 0;

	while (s[c] != '\0'; i++)
	{
		if ((i % 2) == 0)
			_putchar (str[i]);
		else
			continue;
	}
	_putchar ('\n);
}
